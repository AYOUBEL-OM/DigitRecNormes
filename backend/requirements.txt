# ATS Recrutement - Backend FastAPI
fastapi==0.109.2
uvicorn[standard]==0.27.1
python-multipart==0.0.9

# --- Configuration & validation ---
pydantic==2.12.5
pydantic-settings==2.13.1
email-validator==2.1.0
python-dotenv==1.0.1

# --- Base de données ---
sqlalchemy==2.0.25
psycopg2-binary==2.9.9
PyMySQL==1.1.0

# --- Sécurité (JWT, mots de passe, TLS deps) ---
python-jose[cryptography]==3.3.0
cryptography==42.0.2
bcrypt==5.0.0
passlib[bcrypt]==1.7.4

# --- Paiements ---
stripe==15.1.0

# --- IA (Groq : quiz, CV, oral) ---
groq==1.1.1

# --- PDF (extraction CV PyMuPDF ; rapports ReportLab) ---
PyMuPDF==1.27.2.2
reportlab==4.5.0

# --- Client Supabase (module app/supabase_client.py ; non câblé au router pour l’instant) ---
supabase==2.28.2
